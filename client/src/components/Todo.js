import React from "react";

function Todo() {
    
    const getTodos = async () =>{
        await fetch()
    }

    return(
        <>
            <button onClick={getTodos}>Get your Todos</button>
        </>
    )
}

export default Todo;